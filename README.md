# ScriptInvestigator
여러 폴더에 흩어져 있는 C# 스크립트(`.cs`)들을 분석하고 관리하기 위한 간단하면서도 강력한 CLI (Command-Line Interface) 툴킷입니다. 배치 스크립트로 제작된 인터페이스를 통해 복잡한 명령어 없이 손쉽게 코드 분석 작업을 수행할 수 있습니다.

## ✨ 주요 기능

-   **대화형 인터페이스**: 사용자는 `run_analysis.bat` 파일을 실행하여 안내에 따라 손쉽게 작업을 선택할 수 있습니다.
-   **C# 스크립트 병합**: 지정된 여러 소스 폴더 내의 모든 `.cs` 파일의 내용을 하나의 텍스트 파일로 합칩니다.
-   **파일 목록 생성**: 모든 `.cs` 파일의 이름과 전체 경로가 포함된 목록을 생성합니다.
-   **코드 구조 분석 (PowerShell)**: 각 `.cs` 파일의 클래스, 변수, 메서드 등 기본 구조를 분석하여 결과를 저장합니다.
-   **특정 폴더 제외**: 분석 과정에서 `\Deprecated\` 경로에 포함된 파일은 자동으로 제외하여 불필요한 작업을 줄입니다.
-   **동시 작업 수행**: 모든 분석 작업을 한 번의 명령으로 동시에 실행할 수 있습니다.

## 📂 파일 구조

```
ProjectInvestigator/
│
├── run_analysis.bat     # 메인 실행 파일 (사용자가 이 파일을 실행)
│
├── scripts/             # 실제 분석 로직을 담은 스크립트 폴더
│   ├── combine_cs.bat
│   ├── list_cs_files.bat
│   └── analyze_cs.ps1
│
└── README.md            # 프로젝트 설명 파일
```

## 🚀 사용 방법

1.  이 저장소를 다운로드하거나 클론합니다.
2.  `run_analysis.bat` 파일을 더블클릭하여 실행합니다.
3.  화면에 나타나는 안내에 따라 아래 정보를 입력합니다.
    -   **[1단계] 출력 경로**: 분석 결과 파일이 저장될 폴더 경로를 입력합니다. (입력하지 않으면 현재 폴더)
    -   **[2단계] 분석 경로**: 분석하고 싶은 C# 소스 코드가 담긴 폴더 경로를 하나씩 입력합니다. 입력이 끝나면 `q`를 누릅니다.
4.  **[3단계] 실행할 작업 선택**: 원하는 분석 작업을 선택합니다 (A, B, C, D).
    -   하나의 작업을 실행하면, 잠시 후 다시 선택 화면으로 돌아와 다른 작업을 이어서 할 수 있습니다.
    -   경로를 처음부터 다시 설정하고 싶으면 `X`를 선택하세요.

## 🔧 커스터마이징

-   분석에서 제외할 폴더 이름을 변경하고 싶다면 `scripts` 폴더 안의 `.bat` 파일과 `.ps1` 파일에서 `"Deprecated"` 부분을 원하는 이름으로 수정하세요.
<img width="1124" height="400" alt="image" src="https://github.com/user-attachments/assets/d01eb859-2c39-4840-81d1-8033b600967e" />
<img width="974" height="679" alt="image" src="https://github.com/user-attachments/assets/85e87407-0e95-496a-8263-e777e71b5d9a" />
<img width="1143" height="623" alt="image" src="https://github.com/user-attachments/assets/e4048d6f-eac6-43f8-8921-393cb314d19b" />
<img width="945" height="645" alt="image" src="https://github.com/user-attachments/assets/904a1417-f49f-417a-be96-077c6f87667d" />

